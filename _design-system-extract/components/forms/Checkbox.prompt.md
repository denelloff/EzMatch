Toggle for match options and table controls ("Display scores", "Live refresh", "Knife round").

```jsx
<Checkbox label="Live refresh" defaultChecked />
```

eZ-Match has no switch component — booleans are always checkboxes.
