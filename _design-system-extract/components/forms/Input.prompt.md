Standard text input. Focus turns the border Ignite orange with a 3px glow ring; never use a blue browser focus outline.

```jsx
<Field label="Server password" hint="Applied as sv_password when the match starts.">
  <Input type="password" placeholder="optional" />
</Field>
```
