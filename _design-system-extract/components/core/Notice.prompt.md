Block message shown above a form or inside a card. Danger notices get `role="alert"` automatically.

```jsx
<Notice tone="danger">Invalid email or password.</Notice>
<Notice tone="warn">This server is not running. Start it, then create a match.</Notice>
```

There is no toast system in eZ-Match — feedback is always inline.
