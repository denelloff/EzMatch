Empty list placeholder. Title states the fact, description states the next step — never an apology.

```jsx
<EmptyState title="Nothing running" description="Start a match from an instance and it shows up here while it is live." />
```
