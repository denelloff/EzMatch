Long-running task bar (agent deploy, demo sync). 6px track, colour carries state: orange running, green done, red failed.

```jsx
<TaskProgress label="Deploying ez-agent to fra-01" percent={64} eta="~40s left" />
```
